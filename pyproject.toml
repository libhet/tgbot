[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tgbot-miniapp"
version = "0.1.0"
description = "Telegram training mini-app logic with session persistence"
authors = [{name = "Auto-generated"}]
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
