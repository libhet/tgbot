[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tgbot"
version = "0.1.0"
description = "Interactive exercise generators and utilities"
authors = [{name = "AutoGenerated"}]
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["."]

